describe('Herný flow', () => {
  it('player joins and answers', () => {
    cy.visit('/');
    // This is only a placeholder test demonstrating Cypress usage.
    cy.contains('Spoznajme sa');
  });
});
